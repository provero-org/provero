Harden error and edge-case paths across the engine: alert env-var resolution no longer crashes the CLI and `AlertConfig` validates URL and trigger; SLA parsing for freshness and completeness emits a violation instead of raising; anomaly detectors guard against zero thresholds and non-finite history; the profiler no longer misclassifies `INTERVAL` columns as numeric; the optimizer alias mapping is now injective and `row_count`/NULL semantics align between the optimized and standalone paths; dbt range export rejects non-finite bounds; and connections support the context-manager protocol.
